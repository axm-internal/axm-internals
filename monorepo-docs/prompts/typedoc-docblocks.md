# Prompt: Typedoc Docblocks

You are updating TypeScript docs for Typedoc.

Task:
- Ensure all public classes, methods, functions, and properties have complete TSDoc comments.
- Each method/function must include: summary, `@param` for each parameter, `@returns`, and a short `@remarks`.
- Add `@example` when usage isn’t obvious.
- Keep text concise, factual, and aligned with actual behavior.

Output:
- Modify the code to include the docblocks.
