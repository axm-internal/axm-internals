Generate or update <%= it.packageName %>/llms.txt using the repository conventions.

Inputs to read:
- <%= it.packageName %>/README.md
- <%= it.packageName %>/docs/ (Typedoc output)
- <%= it.packageName %>/src/index.ts (public exports)
- <%= it.packageName %>/package.json (name, version)

Requirements:
- Start with a top-level heading using the package name from package.json.
- Include a "Version: x.y.z" line from package.json.
- Provide these sections with short, factual bullets:
  - Purpose
  - Public surface
  - Intended usage
  - Non-goals
  - Stability expectations
- Ensure content matches actual exports and README scope.

Output:
- Output only the full llms.txt content (no preface, no explanation).
